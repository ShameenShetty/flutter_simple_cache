import 'package:flutter_test/flutter_test.dart';

import 'package:flutter_simple_cache/flutter_simple_cache.dart';

void main() {
  test('adds one to input values', () {
    final simpleCache = FlutterSimpleCache.instance;
    // expect(calculator.addOne(2), 3);
  });
}
